1. main auto-sync — store CLAUDE.md after main commit:
# edited in main

2. peer commit — divergence nudge on commit stderr:
   nudge emitted
   store CLAUDE.md after peer commit (still canonical = main):
# edited in main

3. no-op — store commit count unchanged after non-brain commit:
   store unchanged (no spurious commit)

4. deliberate promotion — store CLAUDE.md after 'clc save' in peer:
# edited in peer
