1. main auto-sync — store CLAUDE.md after main commit:
# edited in main

2. peer ahead — promotion notice on commit stderr:
   promotion notice emitted
   store CLAUDE.md after peer commit (promoted):
# edited in peer
   main CLAUDE.md after peer commit (mirrored into trunk):
# edited in peer

3. peer diverged — nudge on commit stderr:
   reconcile nudge emitted
   store CLAUDE.md after diverged peer commit (still main's):
# main v2

4. no-op — store commit count unchanged after non-brain main commit:
   store unchanged (no spurious commit)
