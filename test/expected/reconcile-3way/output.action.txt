reconcile from the peer (brain missing → behind; main in sync):
Reconcile
  current worktree ↔ store
    ↓ .claude/settings.json (behind — store is newer)
    ↓ CLAUDE.md (behind — store is newer)
  main worktree ↔ store
    in sync

  Resolve everything from here:  clc reconcile --apply
  Promote local edits:           clc save
  Take the stored brain:         clc restore
